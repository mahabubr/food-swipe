import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '90%',
        marginVertical: 30,
        marginLeft: 'auto',
        marginRight: 'auto'
    },
    inputField: {
        width: '100%',
        borderWidth: 1,
        borderColor: '#000000',
        padding: 10,
        borderRadius: 30,
    }
})

export default styles